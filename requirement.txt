# Core dependencies
dbus-python>=1.3.2
PyGObject>=3.42.0
pyserial>=3.5

# Bluetooth/DBus
gi-docgen>=2023.1
libdbus-glib>=0.112

# Optional but recommended for better performance
python-dotenv>=1.0.0  # For environment variable management
coloredlogs>=15.0.1  # For better log formatting

# Development dependencies (optional - for testing)
pytest>=7.4.0
pytest-mock>=3.11.1
coverage>=7.3.0